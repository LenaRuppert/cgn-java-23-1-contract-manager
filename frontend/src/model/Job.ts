export type Job = {
    id?: string,
    title: string,
    description: string,
    street: string,
    houseNumber: string,
    postalCode: string,
    city: string,
    orderDate: string,
    clientId: string
}
